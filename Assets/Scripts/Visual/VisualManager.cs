using UnityEngine;
using System.Collections;

// Manager responsible for visual stuff, state transitions etc. 
[RequireComponent (typeof (Animator))]
public class VisualManager : MonoBehaviour {

	public void SetState(string stateName) {
	}
}
